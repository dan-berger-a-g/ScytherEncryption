using System;

namespace ScytherEncryption

public class Random
{
	public string Random(byte, Random)
	{
		var bytes = new byte[9];
		int Key
		rand.Nextbytes(bytes);
		foreach (byte byteValue in bytes)
        {
			(Key = {0, 9}, byteValue)
        }


	}
}
